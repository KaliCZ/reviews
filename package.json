{
  "name": "reviews",
  "private": true,
  "scripts": {
    "dev": "npm run dev:infra && concurrently --kill-others-on-fail -n api,web -c blue,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "dev:infra": "docker compose --env-file .env up -d postgres redis azurite zitadel",
    "dev:infra:down": "docker compose --env-file .env down",
    "dev:api": "dotnet watch --project api",
    "dev:web": "npm --prefix web start",
    "aspire": "dotnet run --project apphost",
    "compose:up": "docker compose --env-file .env up --build",
    "compose:down": "docker compose --env-file .env down -v"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
