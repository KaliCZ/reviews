{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "ConnectionStrings": {
    "reviews": "Host=localhost;Database=reviews;Username=postgres;Password=postgres;Search Path=reviews",
    "cache": "localhost:6379",
    "_comment_temporal": "Fallback for `npm run dev` (local dotnet watch + compose infra on the canonical port). Aspire injects ConnectionStrings__temporal at a worktree-specific port and that env-var override wins via standard config precedence.",
    "temporal": "localhost:7233"
  }
}
