{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Auth": {
    "_comment": "Production defaults — IssuerUrl/Audience must be overridden by the orchestration layer (compose env, Aspire, K8s secrets). RequireHttps=true is the safe default; the dev appsettings overrides it to false for local HTTP.",
    "RequireHttps": "true"
  }
}
