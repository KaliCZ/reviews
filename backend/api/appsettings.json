{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "WEB_ORIGIN": "http://localhost:4200",
  "Auth": {
    "_comment": "Audience comes from zitadel-bootstrap (KeyPerFile Auth__Audience). IssuerUrl defaults to the local-dev ZITADEL; prod overrides it via env var (Auth__IssuerUrl). RequireHttps=true is the safe default; the dev appsettings overrides it to false for local HTTP.",
    "IssuerUrl": "http://localhost:8080",
    "RequireHttps": "true"
  }
}
